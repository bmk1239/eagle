name: update2

on:
  schedule:
    - cron: '0 3 * * *'   # 06:00 Israel time during DST
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      IL_PROXY:        ${{ secrets.IL_PROXY }}
      IL_PROXY_CA_B64: ${{ secrets.IL_PROXY_CA_B64 }}
      # optional – only if you need login cookies
      # IL_FTV_COOKIES: ${{ secrets.IL_FTV_COOKIES }}

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install deps
      run: |
        python -m pip install --upgrade pip
        pip install requests cloudscraper 'backports.zoneinfo;python_version<"3.9"'

    - name: Generate EPG
      run: python script2.py

    - name: Commit & push if updated
      env: { TZ: UTC }
      run: |
        git config user.name  "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add freetv_epg.xml
        if git diff --cached --quiet; then
          echo "No changes."
        else
          git commit -m "Update EPG $(date -u '+%Y-%m-%d %H:%M:%S')"
          git push
        fi
